pipeline {
    agent any

    stages {
        stage('Checkout stage') {
            steps {
                echo 'Cleaning repo'
                echo "pull code from github"
            }
        }
    }
}

